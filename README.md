# Zero Friction Second Brain

Obsidian + Gemini AI 기반 **원클릭** 자동 지식 관리 시스템

> **"쓰기만 해라, 정리는 AI가 한다"**

## 주요 기능

### 핵심 기능
- **프로젝트 자동 분류**: 노트를 Projects/Library/Archives로 자동 분류
- **자동 OCR**: PDF/이미지 자동 감지 → 텍스트 추출 → 프로젝트 분류
- **URL 가져오기**: YouTube/웹페이지 → AI 요약 + iframe 임베드
- **관련 노트 연결**: Embedding 기반 의미 유사도 검색
- **스마트 분리**: 하나의 메모를 프로젝트별로 분리
- **ZK 추출**: 노트에서 Zettelkasten 영구 노트 자동 생성

### 고급 기능
- **RAG 챗봇**: 노트 기반 Q&A (나만의 Second Brain과 대화)
- **캘린더 뷰**: 노트/일정/회의록을 날짜별로 시각화
- **리뷰/회고**: 기간별 노트 분석 + AI 인사이트
- **일정 감지**: 노트에서 일정 자동 추출 → Daily Notes / Google Calendar
- **회의록 분석**: 오디오 파일 → AI 전사 + 요약 + 액션 아이템
- **템플릿 커스터마이징**: YouTube/웹페이지 노트 템플릿 사용자 정의

### 사용자 경험
- **온보딩 마법사**: 첫 설치 시 3단계 가이드
- **빠른 시작 프리셋**: 기본/파워/실험 모드 원클릭 설정
- **모바일 동기화**: 데이터를 볼트 내부에 저장하여 Obsidian Sync 지원

## 요구사항

- Obsidian 1.0.0+
- Gemini API Key (무료)

## 설치

### BRAT 설치 (권장)

1. [BRAT](https://github.com/TfTHacker/obsidian42-brat) 플러그인 설치
2. BRAT 설정 → Add Beta Plugin
3. `yuio124/zero-friction-brain_plugin_TW` 입력

### 수동 설치

1. [Releases](https://github.com/yuio124/zero-friction-brain_plugin_TW/releases)에서 최신 버전 다운로드
2. `main.js`, `manifest.json`, `styles.css`를 복사
3. Vault의 `.obsidian/plugins/zero-friction-brain/` 폴더에 붙여넣기
4. Obsidian 재시작 → 설정 → Community Plugins → 활성화

## 사용법

### 단축키

| 단축키 | 기능 |
|--------|------|
| `Ctrl+Shift+G` | **프로젝트 분류** (PDF/이미지 자동 OCR) |
| `Ctrl+Shift+U` | **URL 가져오기** (YouTube/웹페이지) |
| `Ctrl+Shift+L` | **관련 노트 연결** |
| `Ctrl+Shift+D` | **스마트 분리** (메모를 프로젝트별로 분리) |
| `Ctrl+Shift+S` | 스마트 처리 (OCR+분류) |
| `Ctrl+Shift+O` | OCR 추출 |
| `Ctrl+Shift+Z` | ZK 아이디어 추출 |
| `Ctrl+Shift+R` | **리뷰/회고** |
| `Ctrl+Shift+C` | **캘린더 뷰** |
| `Ctrl+Shift+'` | **AI 챗봇** |
| `Ctrl+Shift+;` | Focus Top 3 추천 |

### 리본 아이콘

- 🧠 아이콘 클릭 → 명령 리스트 모달 (모든 명령 + 단축키 표시)
- 📅 캘린더 아이콘 → 전체화면 캘린더 뷰
- 🤖 봇 아이콘 → RAG 챗봇 사이드바

### 처리 흐름

```
PDF/이미지 → Ctrl+Shift+G → OCR → 마크다운 생성 → 프로젝트 분류 → 폴더 이동
마크다운   → Ctrl+Shift+G → 프로젝트 분류 → 폴더 이동
YouTube    → Ctrl+Shift+U → Gemini 영상 분석 → 요약 노트 + iframe 임베드
웹페이지   → Ctrl+Shift+U → URL Context Tool 분석 → 요약 노트 + iframe 임베드
```

## 설정

### 필수
- **Gemini API 키**: [Google AI Studio](https://aistudio.google.com/apikey)에서 발급

### 폴더 구조 (기본값)
```
00 _Inbox/        # 새 파일 수집
01 Projects/      # 프로젝트별 하위 폴더
   ├── 스마트팜/
   └── 앱개발/
02 Library/       # 프로젝트에 속하지 않는 참고자료
03 Archives/      # 완료/비활성 항목
10 Zettelkasten/  # 원자적 노트
```

### 시간대 및 날짜 형식 (v1.0.20+)
- **시간대**: 자동 감지 / 한국(KST) / 일본 / 미국 등
- **날짜 형식**: ISO / 로컬 / 친근한 형식

### OCR 안전 제한
- 최대 페이지 수: 20 (기본)
- 최대 파일 크기: 10MB (기본)
- 일일 한도: 50페이지 (기본)

## 기술 스택

- TypeScript + esbuild
- @google/genai SDK (Gemini 3 Flash Preview)
- URL Context Tool (웹페이지 분석)
- pdfjs-dist (PDF 처리)

## 프로젝트 구조

```
zero-friction-second-brain/
├── obsidian-zero-friction-brain/  # Obsidian 플러그인 소스
│   ├── src/
│   │   ├── main.ts         # 플러그인 메인
│   │   ├── settings.ts     # 설정 UI
│   │   ├── types.ts        # 타입 정의
│   │   ├── api/            # Gemini API
│   │   ├── core/           # 핵심 로직 (OCR, MOC, ZK 등)
│   │   ├── sources/        # 외부 소스 (YouTube, 웹페이지)
│   │   └── utils/          # 유틸리티 (datetime 등)
│   └── main.js             # 빌드 결과
├── src/                    # Python CLI (레거시)
├── brain.py                # Python CLI 진입점
└── CLAUDE.md               # 개발 가이드
```

## 라이선스

MIT License
